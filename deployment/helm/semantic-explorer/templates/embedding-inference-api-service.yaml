{{- if .Values.embeddingInferenceApi.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "semantic-explorer.fullname" . }}-embedding-inference-api
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "semantic-explorer.embeddingInferenceApi.labels" . | nindent 4 }}
  {{- with .Values.embeddingInferenceApi.service.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.embeddingInferenceApi.service.type }}
  ports:
    - port: {{ .Values.embeddingInferenceApi.service.port }}
      targetPort: {{ .Values.embeddingInferenceApi.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "semantic-explorer.embeddingInferenceApi.selectorLabels" . | nindent 4 }}
{{- end }}
